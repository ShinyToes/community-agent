# 工作记录

## 2026-09-17 正式富文本图文编辑

- 功能提交13d8339已快进合并并推送main，同时推送feature/rich-media与markdown-phase1基线标签；远程ref已核对。

- 用户确认采用富文本并增加配图/字色。feature/rich-media从试验分支开发，markdown-phase1标签保留77c2fdc基线。
- 新增0003迁移、结构化正文验证/安全类样式渲染、图片解码重编码及按当前公开版本授权。前端支持上传/拖放/粘贴、颜色、背景、字号、下划线、删除线和图片宽度；移除Markdown beta转换依赖。
- 大文件参数化测试初次因pytest将5MB参数用于临时路径而报错，补显式短id后SQLite50项/MySQL50项通过。MySQL一条缓存权限警告；临时实例已清理。
- 浏览器发现颜色select标签拼写错误，修正；多段落颜色断言使用唯一匹配后完整浏览器验收通过。查看桌面/手机截图后将标签与选择器成组，并同步选中样式状态。
- 原5001源码和数据库备份至instance/snapshots，迁移到0003；升级前后已有账号摘要及帖子/版本数量一致。正式5001浏览器复测通过，无JS错误。未替换原.env.community与Git历史。
- 后补重复图片节点总数/结构约束，13项相关SQLite用例通过。新docs/rich-media.md说明限制、数据关系及回退边界。

## 2026-09-17 GitHub基线及富文本试用

- main基线77c2fdc、富文本功能提交2f2a494均已推送并通过ls-remote核对；未合并试验分支。

- 源码178文件以新根提交77c2fdc推送指定空仓库main，不带旧实验Git历史/报告截图/密钥/数据库/上传资料。原工作目录未切换分支或覆盖文件。
- 新独立目录community-agent-richtext建立experiment/rich-text，安装锁定Tiptap3.31.3与esbuild0.28.2。TOAST UI官方仓库已归档，未采用该候选。
- 构建首次因沙箱父目录读取权限失败，批准后成功；新仓库首次提交缺身份，沿用原项目已有提交身份，仅设置本仓库，随后推送成功。
- 5002富文本与13309试用MySQL及独立Cookie隔离于原服务；Markdown存储和原安全渲染保留。加载/模式切换不主动改写源文本。
- 首次浏览器运行早于监听就绪，ready通过后继续；测试发现setEditable默认发出update导致未编辑内容重排，改为setEditable(..., false)后测试通过。
- 浏览器标题/加粗/表格/模式切换/精确保留/重载/发布/手机检查通过，无JS错误；37项SQLite通过。原5001仍可用。

## 2026-09-17 实时 Markdown 预览

- 按用户要求改为正文输入暂停350毫秒后自动预览，中文输入法组字期间不发送；已有内容打开时自动预览，保留手动刷新入口。
- 取消过期请求并检查请求序号，避免旧响应覆盖新输入；预览错误只显示在预览状态，不覆盖保存/冲突提示。清空正文会清空旧预览。
- 已重启服务；JS语法及无头Edge自动刷新、清空、已有帖初始预览和原有完整发帖流程通过，无JS错误。说明文档已更新。

## 2026-09-17 阶段 1 内容发布完成

- 25个阶段0源码文件已保存快照，安装markdown-it-py和nh3；新建帖子、不可变版本、标签关系、当前公开片段和操作回执模型，扩展审计并添加0002迁移。
- 实现私有草稿、发布、编辑、软删除、本人历史、首页游标分页、安全Markdown与就地错误提示；按钮明确公开更新，冲突保留输入。所有写操作校验作者、账号状态及基础版本，事务内更新索引和审计，UUID回执处理重试。
- 一次补丁因同一文件delete/add组合被拒绝，拆分为update后成功；阶段0迁移表清单断言随新增表更新；SQLite有数据的循环外键降级改为先清空指针再删表，避免重建被引用表。
- SQLite37通过；独立临时MySQL37通过（45.04s），临时测试项目已清理。MySQL验证包含并发编辑、并发幂等发布及30,000中文正文。
- 正式community_agent库已备份到instance/snapshots/community-before-phase1-20260917-144752.sql后增量升级；5001服务重启，原账号保留，旧学籍数据未操作。
- 无头Edge两账号流程通过：草稿不可见、发布可见、越权拒绝、预览无脚本执行、编辑冲突保留输入、历史、取消/确认删除及移动端无溢出；没有JS错误。查看桌面/手机截图确认可读；虚构验收帖子软删除、账号停用。
- 首页、posts和ready均200，实际版本0002_posts；pip check、Python编译和JS语法检查通过。新增阶段1使用与验收文档，更新README/设计进展。

## 2026-09-17 注册错误就地提醒

- 重复注册改为异步提交，在用户名下方显示红字，保留用户名和密码输入、聚焦用户名，编辑后清除旧错误；提交期间禁用按钮防止重复提交。
- JSON接口仍返回409；未启用JavaScript时重复用户名返回注册表单并保留用户名，不再显示通用错误页。
- 本地服务已重启。无头Edge验证重复注册没有文档跳转、两个输入值保留、重试按钮可用、编辑后提示消失；正常注册/登录/退出及原有页面检查通过，无JS错误。

## 2026-09-17 密码最短长度调整

- 按用户要求将注册、登录和管理员创建的最低密码长度由12改为6；同步表单约束、提示与运行说明。
- 调整已有测试，覆盖6位密码注册/登录和管理员创建；原有5位密码拒绝用例保留。社区24项测试通过。
- 批量替换中Unicode连接号受PowerShell管道编码影响未匹配，检查后通过apply_patch修正提示。

## 2026-09-17 社区阶段 0 完成

- 已保存并验证 132 文件源码快照，未提交或覆盖原 Git 工作区，未归档密钥/私有上传资料。
- 新增 community 包、独立配置/Compose、迁移、注册登录、管理员命令、安全响应和页面；新增社区启动及测试入口。旧业务代码保持原状，旧测试入口仅增加社区目录排除。
- Alembic 联网安装首次受沙箱限制，升级执行后成功；Docker Desktop 和独立13307 MySQL启动成功。
- 测试临时目录与包名称冲突均已修正：SQLite24通过，MySQL24通过（独立测试项目，测试卷已移除）。迁移升级/回滚/重复执行与元数据一致性通过。
- 无头Edge注册/退出/登录/越权拒绝与390px视口检查通过，无JS错误；审阅截图后修正移动端账号换行，复测通过；两个虚构验收账号均停用。
- 旧SQLite测试68通过/5跳过，754条历史警告；pip check、编译、git diff --check、PowerShell启动脚本语法检查通过。
- 社区Web进程已在本机5001启动；首页及两个健康接口均200，SQL确认数据库为community_agent。密钥和快照均确认被Git忽略。
- 阶段文档与README已更新；下一阶段是帖子版本、草稿、标签、发布与安全Markdown，尚未实现。

## 2026-09-17 实施前环境核查

- 已读社区方案并检查现有应用、依赖、配置和 Git 状态。
- .venv-dev 的核心依赖导入、pip check 通过；旧 .venv 启动失败。Docker 引擎查询失败，用户 Docker 配置读取被拒绝；未做安装、重启或数据库写入。
- 新增 docs/environment-check.md，记录环境缺项及下一步阶段 0。当前仅完成用户要求的先行环境核查，未宣称完成社区实现。

## 社区转向与重命名（2026-09-16）

- 用户授权先出第一版方案、覆盖旧设计、改项目名；本轮未实现社区业务。
- 目录改为 community-agent；README 与 docs/agent-design.md 已换为社区方案，历史审查及学籍实施记录保留。
- 新设计明确帖子版本、评论/点赞/收藏、举报治理、私有资料草稿、公开内容问答与引用校验、本人修改建议、隔离新库、阶段验收。
- 沙箱读取进程信息被拒绝后申请权限；移动被旧 .pytest_cache 权限打断，核对部分移动状态后使用目录原子移动继续完成，无文件覆盖/删除。旧缓存根保留在 instance/legacy-cache-root。
- 旧 Web/Worker 当前无运行进程；未改变旧 Compose、数据库、数据卷或密钥。社区正式编码时另建配置和数据库。
- 验证：新目录 Python/SQLAlchemy 导入成功，旧目录不再存在，README 本地链接均有效，git diff --check 通过。本轮只调整文档和目录，没有运行或宣称社区功能测试。

- 已列出业务代码与 SQL，开始静态审查。
- 数据库配置继承原实验，验证使用隔离环境，不运行 seed 或原数据库初始化脚本。
- 已实现：独立 .env.agent 配置、关闭默认公开 debug；全局 CSRF、POST 登出、安全跳转、停用会话校验；学生/统计/附件权限；私有存储及事务回滚清理；转专业身份绑定与状态检查；成绩整批校验、零权重、无中间提交；选课统一过程入口与过程重选检查。
- 环境：原 .venv 启动失效；pip 沙箱网络失败后通过批准的网络升级安装至 .venv-dev。未连接任何原数据库。Docker daemon 不可用，MySQL 实际联调待独立实例。
- 测试环境问题：默认 pytest 临时目录无权限；改为项目 instance/test-runs，下级目录缺父目录后已补齐。
- 一次补丁因 import 行不匹配未应用，核对实际文件后已修正。

## 最终检查

- 运行：.venv-dev/Scripts/python.exe tools/run_tests.py --disable-warnings
- 结果：47 passed，9.28s；441 条旧 Query.get 等弃用警告，非断言失败。
- Python compileall：通过；pip check：No broken requirements found。
- git diff --check：通过（仅 CRLF 提示）。
- 测试阶段仅 SQLite 内存库、虚构数据和 instance/test-runs 中文件；未运行 MySQL DDL/DML。
- 新增 README、审查报告、独立配置样例、安全账号创建脚本、虚构演示数据脚本和测试入口。
- 额外错误：一次 PowerShell 管道中中文匹配导致 substring 未找到，SQL 修改未应用，改用 apply_patch 修复；一次 rg 的 Windows 通配符路径错误，后续使用目录搜索；文档补丁匹配失败后核对并修正。
- 本轮完成；MySQL 实际联调、完整业务规则、迁移与性能任务详见 docs/review-2026-09-16.md。
补充检查：临时禁用 core.autocrlf 会把现有 CRLF 全部报为尾随空白；恢复仓库原有换行设置后 diff --check 再次通过，未修改 Git 配置。
2026-09-16 Agent 设计：已完成 docs/agent-design.md 和 README 入口；按用户补充覆盖多业务文档和多格式，明确 Profile、预览版本、确认事务、查询权限、状态/接口/数据模型与阶段验收。仅修改文档，未执行实现或数据库操作。

## 2026-09-16 Agent 实施完成记录

- 新增七张 Agent 表、五个业务 Profile、多格式解析和后台租约、预览/版本校验/确认事务、来源变更日志、查询与修改草稿、完整页面。
- 独立 MySQL 8.4 绑定 127.0.0.1:13306，虚构演示数据及本地随机口令；未接触原库。修复初始化 SQL_SAFE_UPDATES 以及例程/表字符集排序规则差异。
- 实际 MySQL 全套73测试通过；SQLite68通过/5项MySQL专属跳过。包括追加的所有权、旧版本、取消租约、原文证据、前导零及各业务异常测试。
- 首次新增测试因切换账号前未登出失败，修正测试流程后通过。
- 真实 DeepSeek 查询、草稿及非标准表头映射通过；初次奖惩标题误映射被校验拦截，补充业务语义/已占用字段限制后通过。
- OCR镜像完成。原始PNG误识别通过文本块模式改善；扫描PDF仍误识别学号和90，诚实保留失败结果与人工核对限制；未猜测补正或入库。
- 无头Edge验证管理员查询、上传、Worker预览、取消、学生权限页面通过，无JS错误；已看截图并改进中文标签与来源展示。
- Web与Worker后台启动；系统脚本策略阻止直接执行PS1后，改为只对当前进程生效的参数，未改系统策略。
- README、实施差异记录、设计状态和五类虚构导入样例更新。原设计的完整评测、复杂扫描、可视化映射、更多业务未宣称完成。
- 文档批量补丁第一次因同文件delete/add合并被拒绝，拆分后成功。
